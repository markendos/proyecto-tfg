name: Preproduction Branch
on:
  pull_request:
    branches: [ pre ]
  push:
    branches: [ pre ]
jobs:
  build:
    name: Test, package
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set up JDK
        uses: actions/setup-java@v1
        with:
          java-version: 11
      #- name: Maven Test
      #  run: mvn -B -Dmailsecret=" clean test
      - name: Maven Verify
        run: mvn -B -Pprod "-Dmailsecret=${{ secrets.GOOGLE_APP_PASSWORD }}" clean verify -DskipTests
  artifact:
    name: Publish Artifact 
    runs-on: ubuntu-latest
    needs: [build]
    steps:
      - 
        uses: actions/checkout@v2
      - 
        name: Set up JDK
        uses: actions/setup-java@v1
        with:
          java-version: 11
      - 
        name: Publish artifact on GitHub Packages
        run: mvn -B -Pprod "-Dmailsecret=${{ secrets.GOOGLE_APP_PASSWORD }}" clean deploy -DskipTests
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}          
  docker:
    name: Publish Docker Image
    runs-on: ubuntu-latest
    needs: [build, artifact]
    steps:
      -
        name: Login to Docker Hub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      -
        name: Build and push
        id: docker_build
        uses: docker/build-push-action@v2
        with:
          push: true
          tags: ${{ secrets.DOCKER_REPO }}
      -   
        name: Image digest
        run: echo ${{ steps.docker_build.outputs.digest }}
  automerge:
    runs-on: ubuntu-latest
    needs: [build, artifact, docker]
    steps:      
      - name: Automerge
        uses: "pascalgn/automerge-action@v0.13.1"
        env:
          GITHUB_TOKEN: "${{ secrets.GITHUB_TOKEN }}"
          MERGE_LABELS: ""
